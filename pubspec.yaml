name: first_test_5
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  # additional package
  firebase_auth: ^0.18.4+1
  provider: ^4.3.2+3
  firebase_core: ^0.5.3
  cloud_firestore: ^0.14.4
  firebase_storage: ^5.2.0
  video_player: ^1.0.1
  camera: ^0.5.8+5
  fluttertoast: ^7.1.6
  path_provider: ^1.6.14
  flutter_video_compress: ^0.3.7+8
  intl: ^0.16.1
  flutter_calendar_carousel: ^1.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/background.jpg
    - assets/images/logo.png
    - assets/videos/FT.mp4
    - assets/videos/FT2.mp4
    - assets/videos/LT.mp4
    - assets/videos/Ot.mp4
    - assets/videos/WK.mp4

